# Ethereum RPC URL (Sepolia testnet)
# Get your free API key from: https://www.alchemy.com
PUBLIC_RPC_URL=https://eth-sepolia.g.alchemy.com/v2/YOUR_API_KEY

# Chain ID (11155111 for Sepolia)
PUBLIC_CHAIN_ID=11155111

# IPFS Provider (pinata recommended)
# Options: pinata, node, filecoinPin
PUBLIC_IPFS_PROVIDER=pinata

# Pinata JWT token (required when using pinata)
# Get your free JWT from: https://pinata.cloud
PUBLIC_PINATA_JWT=your_pinata_jwt_here

# Optional: IPFS node URL (only needed if PUBLIC_IPFS_PROVIDER=node)
PUBLIC_IPFS_NODE_URL=

# ERC-8004 Activity Feed API Credentials (SERVER-SIDE ONLY - never exposed to client)
# These are used by server-side proxy routes in /api/activity/*
# The API is polled every 15 seconds for new events
API_USERNAME=admin
API_PASSWORD=your_password_here
